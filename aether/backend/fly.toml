# Fly.io configuration for Aether Backend
# Change 'aether-api' to your desired app name

app = 'aether-api'
primary_region = 'mad'  # Madrid - closest to Spain

[build]
  dockerfile = 'Dockerfile'

[env]
  PORT = '3000'
  NODE_ENV = 'production'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1

# Health check
[[services]]
  protocol = 'tcp'
  internal_port = 3000

  [[services.ports]]
    port = 80
    handlers = ['http']

  [[services.ports]]
    port = 443
    handlers = ['tls', 'http']

  [[services.http_checks]]
    interval = 10000
    grace_period = '10s'
    method = 'get'
    path = '/'
    protocol = 'http'
    timeout = 2000
